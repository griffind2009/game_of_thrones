<%= form_for @character do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :house %>
  <%= f.text_field :house %>

  <%= f.label :img_url %>
  <%= f.text_field :img_url %>

  <%= f.submit "Create" %>
<% end %>
